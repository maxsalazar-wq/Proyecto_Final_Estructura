graph TD;
    Usuario((Usuario)) -->|Teclado/Mouse| UI[VentanaJuego]
    
    subgraph "Paquete: juego"
        Main -->|Inicia| UI
    end

    subgraph "Paquete: logica"
        UI -->|Exploración (Recursivo)| Buscador
        UI -->|Deshacer (Stack)| Historial
        UI -->|Ordenar (QuickSort)| Ordenador
        Jugador -->|Gestiona| Inventario
    end

    subgraph "Paquete: modelos"
        UI -->|Visualiza| Jugador
        UI -->|Combate (PriorityQueue)| Enemigo
        Inventario -->|Contiene| Objeto
        Historial -->|Guarda| Objeto
    end

    style UI fill:#f9f,stroke:#333,stroke-width:2px
    style Main fill:#bbf,stroke:#333
